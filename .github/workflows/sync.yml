name: Donghua Auto-Sync (Turbo)

on:
  schedule:
    - cron: '0 * * * *' # Jalan tiap 1 jam sekali (biar aman)
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Dependencies
        run: npm install axios @supabase/supabase-js

      - name: Run Turbo Warmup (Latest 5 Pages)
        run: node scripts/bulk-warmup.js 5
        env:
          VITE_SUPABASE_URL: https://spuwbbpzwsfkwhinueuq.supabase.co
          VITE_SUPABASE_ANON_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InNwdXdiYnB6d3Nma3doaW51ZXVxIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjY5MTQ3MTcsImV4cCI6MjA4MjQ5MDcxN30.M_IjAGI94ETG5kE7zmt-Qyg-iN3Ru86DHCH7igqOMIw